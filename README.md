## Satellite Images
Satellite images are programmatically fetched using latitude and longitude coordinates via a map API.
Due to size constraints, images are not included in this repository.
Use `data_fetcher.py` to download the images before training.
